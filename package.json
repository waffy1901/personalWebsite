{
  "private": true,
  "workspaces": [
    "main"
  ],
  "scripts": {
    "dev": "npm run dev --workspace main",
    "start": "npm run start --workspace main",
    "build": "npm run build --workspace main",
    "test": "npm run test --workspace main",
    "preview": "npm run preview --workspace main"
  },
  "dependencies": {
    "personal-website": "file:main"
  }
}
